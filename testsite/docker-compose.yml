services:
  testsite:
    image: nginx:alpine
    container_name: omnivore-testsite
    ports:
      - "8765:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
